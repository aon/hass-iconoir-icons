{
  "config": {
    "step": {
      "user": {
        "title": "Iconoir Icons",
        "description": "Enable the Iconoir icon pack (`icon: iconoir:<name>`). After setup, reload your browser once so the icons register."
      }
    },
    "abort": {
      "single_instance_allowed": "Already configured. Only a single instance is possible."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Iconoir Icons",
        "data": {
          "stroke_width": "Stroke width"
        },
        "data_description": {
          "stroke_width": "Thinner reads lighter, thicker reads bolder. Reload the browser after changing (the new width applies to fresh frontend loads)."
        }
      }
    }
  },
  "selector": {
    "stroke_width": {
      "options": {
        "1.0": "1.0 px (thin)",
        "1.25": "1.25 px",
        "1.5": "1.5 px (default)",
        "1.75": "1.75 px (bold)",
        "2.0": "2.0 px (heavy)"
      }
    }
  }
}
